import HomePage from '@/features/home/pages/HomePage';

//INFO: Usually we fetch data here then pass it to child component to affect loading component.
const HomePageRoute = async () => {
  return <HomePage />;
};

export default HomePageRoute;
